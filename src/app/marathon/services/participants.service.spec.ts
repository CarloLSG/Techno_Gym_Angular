import { ParticipantsService } from './participants.service';

describe('ParticipantsService', () => {
  it('should create an instance', () => {
    expect(new ParticipantsService()).toBeTruthy();
  });
});
